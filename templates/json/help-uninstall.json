{
    "command": "uninstall",
    "description": "Uninstalls a package locally from your upt_modules directory",
    "usage": [
        "uninstall <name> [<name> ..] [<options>]"
    ],
    "options": [
        {
            "shorthand":   "-h",
            "flag":        "--help",
            "description": "Show this help message"
        },
        {
            "shorthand":   "-S",
            "flag":        "--save",
            "description": "Save installed packages into the project's upt.json dependencies"
        },
        {
            "shorthand":   "-D",
            "flag":        "--save-dev",
            "description": "Save installed packages into the project's upt.json devDependencies"
        }
    ]
}
